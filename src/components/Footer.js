import { HeaderLogo } from './HeaderLogo';

export const Footer = () => {
    return (
        <footer>
            <HeaderLogo />
            <div>
                © created by <span>username</span> - devChallenge.io 2021
            </div>
        </footer>
    );
};